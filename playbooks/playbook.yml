---
- name: Alessio's Provisioning
  hosts: 127.0.0.1
  connection: local

  vars:
    shell_path: "/bin/zsh"
    current_shell: "{{ ansible_env.SHELL }}"
    sublime_path: "{{ ansible_env.SUBLIME_PATH | default('/Users/osso/Library/Application Support/Sublime Text 3') }}"
    provisioning_path: "{{ ansible_env.PROVISIONING_PATH | default('~/Sync/Provisioning') }}"

  roles:
    - role: debian
      when: ansible_os_family == 'Debian'
    - role: osx
      when: ansible_os_family == 'Darwin'

  tasks:

    - name: Allow homebrew zsh to be selected as default shell
      sudo: true
      lineinfile: dest=/etc/shells line={{ shell_path }}

    - name: Set default shell to {{ shell_path }}
      command: chsh -s {{ shell_path }}
      when: "current_shell != shell_path"

    - name: Create bash conf directory
      file: dest=~/.bash state=directory

    - name: Create zsh conf directory
      file: dest=~/.zsh state=directory

    - name: Create zsh autocomplete plugins directory
      file: dest=~/.zsh/autocomplete state=directory

    - name: Create ipython conf directory (1/2)
      file: dest=~/.ipython state=directory

    - name: Create ipython conf directory (2/2)
      file: dest=~/.ipython/profile_default state=directory

    - name: create homedir symlinks
      file: src="{{ provisioning_path }}/{{ item.from }}" dest="{{ item.to }}" state=link
      sudo: no
      with_items:
        # Bash
        - from: shells/bash/profile
          to: ~/.bash_profile
        - from: shells/bash/bashrc
          to: ~/.bashrc
        # Zsh
        - from: shells/zsh/profile
          to: ~/.zprofile
        - from: shells/zsh/zshrc
          to: ~/.zshrc
        - from: shells/zsh/completion
          to: ~/.zsh/completion

        # Shell colors
        - from: shells/colors
          to: ~/.bash/colors
        - from: shells/colors
          to: ~/.zsh/colors

        # Dir colors
        - from: shells/dircolors
          to: ~/.dircolors

        # Shell prompt
        - from: shells/bash/prompt
          to: ~/.bash/prompt
        - from: shells/zsh/prompt
          to: ~/.zsh/prompt

        # Alias
        - from: shells/alias
          to: ~/.bash/alias
        - from: shells/alias
          to: ~/.zsh/alias

        # Shell env
        - from: shells/env
          to: ~/.bash/env
        - from: shells/env
          to: ~/.zsh/env

        # Shell funcs
        - from: shells/func
          to: ~/.bash/func
        - from: shells/func
          to: ~/.zsh/func

        # Git
        - from: git/gitconfig
          to: ~/.gitconfig
        - from: git/gitignore
          to: ~/.gitignore

        # Mercurial
        - from: hg/hgrc
          to: ~/.hgrc
        - from: hg/hgignore
          to: ~/.hgignore

        # Zsh bookmarks
        - from: shells/zsh/zshmarks/zshmarks.plugin.zsh
          to: ~/.zsh/zshmarks
        - from: shells/zsh/zshmarks/_jump
          to: ~/.zsh/autocomplete/_jump
        - from: shells/zsh/bookmarks
          to: ~/.bookmarks

        # Misc
        - from: inputrc
          to: ~/.inputrc
        - from: wgetrc
          to: ~/.wgetrc
        - from: vimrc
          to: ~/.vimrc
        - from: pylintrc
          to: ~/.pylintrc
        - from: nanorc
          to: ~/.nanorc

        # IPython
        - from: python/invenio.ipython.py
          to: ~/.ipython/invenio.ipython.py
        - from: python/ipython_config.py
          to: ~/.ipython/profile_default/ipython_config.py

        # Path Directories
        - from: bin
          to: ~/bin
        - from: lib
          to: ~/lib

        # Sublime
        - from: sublime/Default (OSX).sublime-keymap
          to: "{{ sublime_path }}/Packages/User/Default (OSX).sublime-keymap"
        - from: sublime/Preferences.sublime-settings
          to: "{{ sublime_path }}/Packages/User/Preferences.sublime-settings"
        - from: sublime/RunMultipleCommands.py
          to: "{{ sublime_path }}/Packages/User/RunMultipleCommands.py"
        - from: sublime/Twilight.tmTheme
          to: "{{ sublime_path }}/Packages/User/Twilight.tmTheme"
        - from: sublime/Package Control.sublime-settings
          to: "{{ sublime_path }}/Packages/User/Package Control.sublime-settings"
        - from: sublime/DetectSyntax.sublime-settings
          to: "{{ sublime_path }}/Packages/User/DetectSyntax.sublime-settings"
        - from: sublime/Snippets.py
          to: "{{ sublime_path }}/Packages/User/Snippets.py"
        - from: sublime/Move.py
          to: "{{ sublime_path }}/Packages/User/Move.py"
        - from: sublime/PythonSnippets.sublime-commands
          to: "{{ sublime_path }}/Packages/User/PythonSnippets.sublime-commands"
        - from: sublime/SublimePython.sublime-settings
          to: "{{ sublime_path }}/Packages/User/SublimePython.sublime-settings"
        - from: sublime/SublimeLinter.sublime-settings
          to: "{{ sublime_path }}/Packages/User/SublimeLinter.sublime-settings"

    - name: easy install pip
      easy_install: name=pip
      sudo: yes

    - name: install apps from pip
      pip: name={{ item }}
      with_items:
        # Virtual environments
        - virtualenv
        - virtualenvwrapper
        - pew
        # CLI awk like python
        - pythonpy
        # Requires by some scripts in ~/bin
        - plumbum
        # Linters for sublime text
        - pep8
        - pylint
        # Always useful
        - ipython

    - name: install powerline for vim
      command: pip install git+git://github.com/Lokaltog/powerline creates=/usr/local/bin/powerline
