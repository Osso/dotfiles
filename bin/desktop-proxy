#!/bin/bash

function connect {
    rm -f /tmp/sfr-docker.sock
    echo 'Redirecting mh:/var/run/docker.sock to /tmp/sfr-docker.sock'
    ssh -N -p 22 -c aes256-ctr osso@desktop -L /tmp/sfr-docker.sock:/var/run/docker.sock
}

connect
rm -f /tmp/sfr-docker.sock
echo 'Deleted /tmp/sfr-docker.sock'


