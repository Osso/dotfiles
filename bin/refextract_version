#!/usr/bin/env python

import re

from invenio.refextract_config import CFG_REFEXTRACT_VERSION

def refextract_version():
    m = re.search(r'refextract/([\d.]+)', CFG_REFEXTRACT_VERSION)
    if m:
        return m.groups()[0]

print refextract_version()
